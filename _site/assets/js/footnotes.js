window.addEventListener("load", function(e){
  footnotesList = document.querySelectorAll(".footnotes")
  for(el of footnotesList){
    notes = el.children[0].children; // the first element is an ol.
  }
})
